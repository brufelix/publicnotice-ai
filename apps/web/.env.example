# URL da API em produção (Railway ou outro host)
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# Modo demonstração sem backend (ideal para Vercel enquanto a API não está no ar)
# true = documentos e chat simulados no browser
NEXT_PUBLIC_USE_MOCK_API=false
